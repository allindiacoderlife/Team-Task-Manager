enum WorkspaceRole {
    ADMIN
    MEMBER
}

enum TaskStatus {
    TODO
    IN_PROGRESS
    REVIEW
    TESTING
    DONE
    BLOCKED
}

enum TaskType {
    TASK
    BUG
    FEATURE
    IMPROVEMENT
    OTHER
}

enum ProjectStatus {
    ACTIVE
    PLANNING
    COMPLETED
    ON_HOLD
    CANCELLED
}

enum Priority {
    LOW
    MEDIUM
    HIGH
}

enum Role {
    USER
    ADMIN
}

enum OTPType {
    LOGIN
    RESET_PASSWORD
}

enum InvitationStatus {
    PENDING
    ACCEPTED
    REJECTED
    EXPIRED
}
